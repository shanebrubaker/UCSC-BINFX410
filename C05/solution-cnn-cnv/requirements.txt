# torch 2.0.x was compiled against NumPy 1.x; NumPy 2.x causes an ABI crash.
# Pin numpy<2 when installing into any existing environment.
# Preferred setup: use environment.yml to create a clean env.
numpy<2
pandas>=2.0
scipy>=1.10
matplotlib>=3.7
seaborn>=0.12
scikit-learn>=1.3
torch>=2.0
jupyter
ipykernel
